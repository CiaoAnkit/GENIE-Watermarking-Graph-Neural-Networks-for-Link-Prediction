This repository contains all the watermarked models and their corresponding graphs for subgraph based method watermarking using GENIE. To check the performance of the watermarked model on test and watermarked dataset for all datasets, run the following code.

```shell
chmod +x run_main.sh
./run_main.sh